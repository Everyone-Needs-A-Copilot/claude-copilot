"""docs_backends — pluggable documentation source backends.

Backends
--------
local   — reads from the installed package on disk (zero network, version-exact)
fetch   — fetches docs from the network (requires httpx optional extra)
"""
