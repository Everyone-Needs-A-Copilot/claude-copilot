{
  "mcpServers": {
    "copilot-memory": {
      "command": "node",
      "args": ["$COPILOT_PATH/mcp-servers/copilot-memory/dist/index.js"],
      "env": {
        "MEMORY_PATH": "$HOME/.claude/memory",
        "WORKSPACE_ID": "$PROJECT_NAME"
      }
    },
    "task-copilot": {
      "command": "node",
      "args": ["$COPILOT_PATH/mcp-servers/task-copilot/dist/index.js"],
      "env": {
        "TASK_DB_PATH": "$HOME/.claude/tasks",
        "WORKSPACE_ID": "$PROJECT_NAME"
      }
    }
  }
}
