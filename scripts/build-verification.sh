#!/bin/bash
# MCP servers (copilot-memory, skills-copilot, websocket-bridge) have been removed.
# Memory and skills are now handled by the cc CLI (tools/cc/).
echo "No MCP servers to verify. Memory and skills are provided by the cc CLI (tools/cc/)."
exit 0
