---
id: 52d9e8c6-5d17-4fa2-84d9-878287c0384a
type: decision
tags: []
created: 2026-07-27T12:26:44Z
updated: 2026-07-27T12:26:44Z
scope: project
---

Defect 2 fix (client id never reached a fresh Mac): added tools/cc/src/cc/core/ecosystem/bootstrap_config.py -- fetch_org_client_id(org) reads a NEW public artifact at https://raw.githubusercontent.com/<org>/copilot-bootstrap/main/bootstrap.yml (org + github_app.client_id only, unauthenticated plain HTTPS via urllib, no gh CLI) via a {org}-template overridable by github_app.bootstrap_url_template config. commands/auth.py's _resolve_client_id() now returns (client_id, error_code): org-required (no org known) vs no-company-app (org known, bootstrap empty/unreachable). Org name resolves via --org flag (cc auth login --org <org>, mirrors onboard.py's existing --org convention) or the github_app.org config key (the inherited-pointer path: app/admin sets it once via cc config set). Removed the dead ecosystem_config.github_client_id() fallback (nothing ever materializes ~/.claude/ecosystem.yml). Outward action still owed by the admin/owner: publish a NEW public repo <org>/copilot-bootstrap with bootstrap.yml containing only org + github_app.client_id -- distinct from the existing private <org>-copilot-internal/ecosystem.yml.
