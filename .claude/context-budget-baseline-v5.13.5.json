{
  "$comment": "Re-baseline for FF9 after two feature commits that the v5.13.4 baseline (captured 2026-08-09) predates. Both tripped FF9, and NEITHER is what its thresholds are designed to catch.\n\nGROWTH -- 14d5791 (2026-08-11) 'wire design/creative agents to the knowledge ladder': cco.md 9,081 -> 10,222 B (1.126x), extensions.md 6,862 -> 8,581 (1.251x), setup-knowledge-sync.md 6,174 -> 8,115 (1.314x). ind, uxd, uids and cco ran `eval \"$(cc env)\"` but never dereferenced any knowledge-repo path, so the owner's own design philosophy never reached them. Each now walks $CC_KNOWLEDGE_REPOS. The added bytes ARE the fix; growth_ratio exists to catch content bloat, and this is function.\n\nSHRINK -- a572b71 (2026-08-12) 'Deliver canonical ecosystem reconciliation and conformance': setup-project.md 13,318 -> 5,391 B (0.405x), update-project.md 14,080 -> 4,455 (0.316x). Procedure moved out of markdown and into the cc CLI (-1,632 md lines, +784 in conformance.py/layers.py/update.py). Both files now state they are 'a human-facing adapter over the canonical cc transaction'. shrink_floor_ratio exists to catch an accidental body-strip or truncated write; this is deliberate prose-to-code extraction, which is the direction the framework should move. The byte counts are not 1:1 because code is denser than prose describing code.\n\nPolicy unchanged: this re-baseline resets captured sizes, not thresholds. The absolute ceilings introduced alongside it are re-set to the newly measured values for the same reason -- a ceiling that lands already breached is a ceiling people disable.",
  "framework_version": "5.13.5",
  "captured_at": "2026-08-15T16:15:29Z",
  "byte_to_token_ratio": 4.0,
  "byte_to_token_basis": "~4 characters per token for English/markdown text -- OpenAI's widely-published tokenizer heuristic (platform.openai.com/tokenizer: 'a helpful rule of thumb is that one token generally corresponds to ~4 characters of text'). No tokenizer dependency is added; this is a documented approximation, applied identically to every artifact so ratios between artifacts stay comparable even though the absolute token count is approximate.",
  "thresholds": {
    "growth_ratio": 1.1,
    "growth_ratio_basis": "Matches IMPL-plan item 5's starting point: tight enough to catch a real content-bloat edit, loose enough for normal edits (a new table row, a paragraph) not to trip it. Applies uniformly to every tracked artifact (agents, commands, CLAUDE.md, the SessionStart injection file, and the frontmatter-description catalog). Unchanged from v5.13.3 -- this re-baseline resets the captured byte counts, not the policy.",
    "shrink_floor_ratio": 0.7,
    "shrink_floor_basis": "A single edit legitimately removing under 30% of a file (trimming a stale section) is normal; losing more than 30% in one change is the signature of an accidental body-strip or truncated write, which a growth-only budget cannot see. Unchanged from v5.13.3.",
    "corpus_ceiling_ratio": 1.1,
    "corpus_ceiling_basis": "Same 10% headroom as the per-file ratio, applied to the SUM of all 16 agent files, so many small under-ratio additions across different files (each individually compliant) still can't add up to unbounded corpus growth. Unchanged from v5.13.3.",
    "always_loaded_growth_ratio": 1.08,
    "always_loaded_growth_ratio_basis": "Deliberately tighter than every other ratio in this file (8% vs 10%) because this is the one number that taxes literally every session with zero user action -- CLAUDE.md and the SessionStart injection are read whether or not the session ever touches an agent or a command. Unchanged from v5.13.3; the Output Contract pass's +1,166-byte CLAUDE.md addition (ratio 1.046 against this re-baseline) still fits inside it without needing to loosen it.",
    "always_loaded_ceiling_bytes": 26268,
    "always_loaded_ceiling_bytes_basis": "ABSOLUTE ceiling, not a ratio. Every other threshold here is a growth ratio, which constrains how fast the burden may rise and is structurally unable to require it to fall -- a framework at 26 KB always-loaded and one at 260 KB are equally compliant with an 8% growth cap, and no sequence of compliant edits can ever bring the number down. Set at the measured value on 2026-08-15, so it costs nothing today and makes every future increase a reviewed decision rather than a compliant 8% step. Raising it is legitimate; doing so silently is not.",
    "agent_corpus_ceiling_bytes": 205345,
    "agent_corpus_ceiling_bytes_basis": "ABSOLUTE ceiling on the summed agent corpus. Every other threshold here is a growth ratio, which constrains how fast the burden may rise and cannot require it to fall; no sequence of individually-compliant edits can ever bring the number down. Set at the MEASURED value on 2026-08-15 (203,126 B) rather than the v5.13.4 baseline (195,975 B), so the gate costs nothing on the day it lands. That 7,151 B gap is itself the point: the corpus drifted 3.6% above baseline through edits that each passed the 1.1x per-file ratio, which is precisely the ratchet an absolute ceiling exists to stop. Raising this is legitimate; raising it silently is not. RAISED 203126 -> 203567 B (+441) on 2026-08-17 for the mandatory `Unknowns:` slot added to the seven design-stage agents' Output Format (sd, uxd, uids, ind, cco, ta, cw). Reason, in full: docs/50-features/surfacing-unknowns.md. Summary: on an identical brief carrying a real contradiction, the no-framework arm asked, earned three sealed clarification answers and priced 25 fewer labour hours; the arm running the full design chain with enforcement verified asked nothing and priced a guess. The chain was resolving internally what should have been a conversation. These are the cheapest bytes available to fix that -- the rationale is deliberately in docs/ rather than in _shared/output-contract.md, which is duplicated into every agent file and would have charged 17x for the same prose. RAISED AGAIN 203567 -> 205345 B (+1778) on 2026-08-17 for the taste-corpus read clause added to the seven agents that already walk $CC_KNOWLEDGE_REPOS. It points at 08-taste/INDEX.md, which exists only in the personal tier (rank 10) and is empty until the owner's own feedback fills it -- so the bytes buy a read path, not content. Placement is deliberate and load-bearing: taste in the org or department tier stops being taste and becomes policy imposed on every inheritor, which SOUL.md's Company Fork anti-pattern refuses. See claude-copilot-private/knowledge/08-taste/README.md."
  },
  "agents": {
    "cco.md": 10539,
    "cpa.md": 11240,
    "cs.md": 9633,
    "cw.md": 10511,
    "do.md": 10527,
    "doc.md": 8216,
    "ind.md": 15913,
    "kc.md": 8580,
    "me.md": 9774,
    "qa.md": 13205,
    "sd.md": 16226,
    "sec.md": 10269,
    "ta.md": 13434,
    "uid.md": 8630,
    "uids.md": 26291,
    "uxd.md": 22357
  },
  "agent_corpus_total_bytes": 205345,
  "agent_corpus_note": "Post-Output-Contract-pass measured total. The pre-pass total (v5.13.3 baseline) was 163,287 bytes; this pass added the 2,042-byte canonical output-contract.md block, byte-for-byte, to all 16 agent files (+32,688 bytes, +20.0%), anchored immediately before '## Runtime Precedence' in each file and verified by FF10.",
  "commands": {
    "config.md": 8735,
    "continue.md": 14169,
    "extensions.md": 8581,
    "knowledge-copilot.md": 2736,
    "map.md": 3894,
    "memory.md": 4881,
    "orchestrate.md": 5118,
    "pause.md": 4639,
    "protocol.md": 38087,
    "reflect.md": 4335,
    "setup-copilot.md": 1320,
    "setup-knowledge-sync.md": 8115,
    "setup-project.md": 5391,
    "setup.md": 7030,
    "skills-approve.md": 2394,
    "update-copilot.md": 3498,
    "update-project.md": 4455
  },
  "always_loaded": {
    "claude_md_bytes": 20531,
    "claude_md_note": "Repo-root CLAUDE.md. Claude Code auto-loads a project's CLAUDE.md into every session unconditionally -- this is documented default behavior and was independently confirmed by inspecting this very session's own injected context. Grew by 1,166 bytes in this pass: a new '## Output Contract' section naming the canonical source, the two-register split, and the `output.verbosity`/`output.audience` config knobs.",
    "session_start_injection_bytes": 3445,
    "session_start_injection_note": "`.claude/hooks/protocol-injection.md`, emitted verbatim as the SessionStart hook's `systemMessage` on every session (session-start.sh), gated only by the `COPILOT_SESSION_START=off` escape hatch. The hook also appends a variable-length 'Known References' block (from `cc config`/`cc memory`, empty on a fresh install) and a 'Session Quota' block (from a usage cache) -- both runtime-computed and excluded here because they are not committed, reviewable file content and can't be measured as a static byte budget the way the rest of this manifest is. Unchanged in this pass.",
    "frontmatter_catalog_bytes": 2292,
    "frontmatter_catalog_note": "Sum of the frontmatter `description:` field bytes across all 16 agent .md files -- the name+description catalog Claude Code loads into the system prompt every session so it knows when to route to a subagent, independent of whether any agent is actually invoked. Unchanged in this pass -- the Output Contract pass touched agent bodies, not frontmatter.",
    "total_bytes": 26268
  },
  "excluded_from_always_loaded": {
    "protocol.md": {
      "bytes": 36624,
      "reason": "Loads only on an explicit `/protocol` invocation (a slash command that requires the user or the session's own convention to type it), not injected unconditionally by a hook or an auto-loaded-file convention the way CLAUDE.md and the SessionStart injection are. It is the single largest command and the framework's own documented default way most sessions begin, so it is still tracked -- under the ordinary per-artifact growth-ratio and shrink-floor invariants in `commands` above, same as every other command -- just not summed into the hard always-loaded ceiling. This pass added the full 2,042-byte Output Contract block (anchored immediately before '## Acknowledge', verified by FF10) and consolidated two duplicate verbosity/token-budget passages into one-line pointers at the contract, netting +2,056 bytes.",
      "verification": "No PreToolUse/SessionStart/UserPromptSubmit hook reads or emits protocol.md's content; grep across .claude/hooks/*.sh for 'protocol.md' returns no reference. It is read only when the user's harness resolves an explicit /protocol slash-command invocation."
    },
    "AGENTS.md": {
      "bytes": 5840,
      "reason": "Codex-layer file (per its own header: 'This project uses the shared codex-copilot framework through the project-local plugin link'). Not referenced via an `@path` import in CLAUDE.md (verified: `grep -n '^@' CLAUDE.md` and `grep -n AGENTS.md CLAUDE.md` both return nothing) and not read by any Claude Code hook under `.claude/hooks/`. Excluded because it is not, in fact, loaded into a Claude Code session at all -- it is Codex's equivalent of CLAUDE.md, consumed by a different harness. Unchanged in this pass -- the Output Contract work was scoped to Claude-side agents/commands only (see repo-root CLAUDE.md's Output Contract section); Codex-side mirrors under plugins/codex-copilot/ were intentionally left untouched, consistent with this branch's recent removal of cross-product Codex plugin contribution."
    }
  },
  "override_log": ".claude/context-budget-overrides.jsonl"
}
